{{- range .Values.services }}
apiVersion: v1
kind: Service
metadata:
  name: {{ . }}
spec:
  selector:
    app: {{ . }}
  ports:
    - protocol: TCP
      port: {{ $.Values.service.port }}
      targetPort: {{ $.Values.service.port }}
---
{{- end }}
